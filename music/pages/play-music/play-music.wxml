<audio poster="{{poster}}" name="{{name}}" author="{{author}}" src="{{src}}" id="myAudio" controls loop>
</audio>
<block wx:for="{{ lrc }}">
  <view>
    <text class='song'>{{ item }}</text>
  </view>

</block>